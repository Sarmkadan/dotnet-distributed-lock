# FencingTokenBenchmark

`FencingTokenBenchmark` is a benchmarking class used to evaluate the performance and correctness of distributed lock fencing token implementations across different backend systems. It measures the behavior of token issuance, validation, and resource lock checks under controlled conditions, ensuring that fencing tokens behave as expected in a distributed environment.

## API

### `BackendType`
